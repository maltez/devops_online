[
  {
    "cpu": ${cpu},
    "image": "${image}",
    "memory": ${memory},
    "name": "${name}",
    "networkMode": "awsvpc",
    "portMappings": [
      {
        "containerPort": ${port},
        "hostPort": ${port}
      }
    ]
  }
]